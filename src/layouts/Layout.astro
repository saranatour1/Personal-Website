---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import NoiseFilter from '../components/NoiseFilter.astro';
import '../styles/globals.css'
import Blob from '../components/Blob.astro';


const { title, descreption, pubDate, updatedDate, heroImage } = Astro.props;
---

<html lang="en">
	<head>
		<BaseHead title={title} description={descreption} />
	</head>
	<body class=" w-full h-full  min-h-screen max-w-full  flex flex-col justify-between items-center pt-52 max-sm:px-4 px-32 max-sm:pt-28 font-recoleta bg-yellow-5 before:absolute before:left-0 before:top-0 before:content-[''] before:w-full before:h-full  before:-z-10  before:opacity-40 after:absolute after:left-0 after:top-0 after:content-[''] after:w-full after:h-full  after:-z-10  after:opacity-40 before:bg-slate-400   before:inset-0 relative ">
		<Blob />
		<slot/>

		<Footer />
		<NoiseFilter />
	</body>
</html>
