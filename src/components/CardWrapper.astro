---
import { WobbleCard } from "./ui/WobbleCard";
interface Props{
  className?:string;
  firstSection?:boolean;
}
const { className, firstSection=false} = Astro.props;
---
<WobbleCard className={className} client:visible={!firstSection} client:idle={firstSection}>
  <slot />
</WobbleCard>
